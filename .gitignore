# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# HTB writeup source material — PRIVATE reference, never publish.
# Raw Notion export, extracted notes, derived screenshots, working manifest.
_drafts/hackthebox.zip
_drafts/hackthebox_extracted/
_drafts/htb_notion/
_drafts/htb_manifest.md

# never ship binaries in a public blog
*.exe
*.dll
*.bin
*.msi
*.so
